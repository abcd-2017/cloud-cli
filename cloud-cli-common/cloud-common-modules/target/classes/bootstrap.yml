spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848

#    sentinel:
#      transport:
#        #配置Sentinel dashboard 地址
#        dashboard: 192.168.213.250:8080
#        #默认8719端口，假如端口被占用 会自动从8719开始依次+1扫描，直至找到未占用的端口
#        port: 8719
